[project]
name = "dbt-bigquery-ai-classify-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "dbt>=1.0.0.40.15",
    "dbt-bigquery>=1.10.2",
]
